Venue: <%= @venue.name %><br>
Address: <%= @venue.address %> <br>

Events at this venue:<br>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Date</th>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= link_to event.name, event %></td>
        <td><%= event.date %></td>
      </tr>
    <% end %>
  </tbody>
</table>
